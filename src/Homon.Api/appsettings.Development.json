{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },

  "//ConnectionStrings": "Deliberately absent. This file is committed, and the development connection string carries a password, so it lives in .NET user secrets instead — see docs/postgres-setup-dev.md.",

  "//Administrator": "Also in user secrets: `dotnet user-secrets --project src/Homon.Api set Administrator:Email you@example.com` and the hash from `-- hash-password` under Administrator:PasswordHash.",

  "FrontEnd": {
    "//": "Empty in development: the Vite dev server proxies /api, so the browser sees one origin and CORS never engages. PublicBaseUrl is the Vite dev server itself — emailed links must land on the SPA, not on Kestrel.",
    "AllowedOrigins": [],
    "PublicBaseUrl": "http://localhost:5300"
  },

  "Email": {
    "//": "No token in development. The application registers LoggingEmailSender instead and writes alerts to the log.",
    "ResendApiToken": null
  }
}
